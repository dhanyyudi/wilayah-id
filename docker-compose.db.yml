version: '3.8'

services:
  postgres:
    image: postgis/postgis:15-3.4-alpine
    container_name: wilayah-db
    environment:
      POSTGRES_USER: wilayah
      POSTGRES_PASSWORD: wilayah123
      POSTGRES_DB: wilayah_id
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wilayah -d wilayah_id"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
